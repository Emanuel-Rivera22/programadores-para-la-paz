app.post('/reporte', (req, res) => {
  const mensaje = req.body.mensaje;
  res.json({
    estado: "Reporte recibido",
    mensaje: mensaje
  });
});

esta ruta lo que hace es que el servidor recibe la peticion de "mensaje" y responde a ese mensaje con "Reporte recibido". 
